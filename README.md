# oracle-code
Oracle Code — local-first terminal coding agent (Bun + TypeScript). Zero-alloc packed-cell TUI, Arabic shaping/bidi-correct grid, JSONL sessions, lazy MCP schemas, checkpoint/permission split.
